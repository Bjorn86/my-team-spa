// IMPORT STYLES
import './SectionTitle.scss';

// SECTION TITLE COMPONENT
function SectionTitle({ titleText }) {
  return <h1 className='section-title'>{titleText}</h1>;
}

export default SectionTitle;
